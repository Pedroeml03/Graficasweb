document.addEventListener('keydown', function(event) {
            if (event.key === "Escape") {
                // Redirigir a la ventana de pausa
                window.location.href = "Pausa.html";  // Asegúrate de que esta ruta sea correcta
            }
        });