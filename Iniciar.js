 function startGame(players) {
            if (players === 1) {
                alert("Iniciando juego para 1 jugador.");
                // Aquí puedes redirigir a otra página o iniciar el juego para un jugador
                window.location.href="BomberRun.html"
            } else if (players === 2) {
                alert("Iniciando juego para 2 jugadores.");
                // Aquí puedes redirigir a otra página o iniciar el juego para dos jugadores
                window.location.href="BomberRun.html"
            }
        }